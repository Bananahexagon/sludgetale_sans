const config = {
    "display_quality": 2,
    "stage_width": 640,
    "stage_height": 480,
    "display_width": 800,
    "display_height": 600,
    "canvas_name": "canvas"
}

type configT = typeof config;

export {
    config,
    configT
}